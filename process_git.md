# Within /data/johnsons_val

    git status
    git diff

    git add folder/file.py folder/folder/

    git diff --staged       # Check content to send

    git commit -m "Description"

    git push origin main


# Create new branch if big changes
# Stay on main otherwise 
